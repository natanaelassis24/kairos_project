#include "glad.h"
#include <GLFW/glfw3.h>

// Esta é uma versão mínima para carregar GL 3.3 Core — para testes.
// Na prática, você deve usar o arquivo gerado pelo site do glad.

int gladLoadGLLoader(GLADloadproc load) {
    // Função stub que só retorna sucesso para testes
    return 1;
}
